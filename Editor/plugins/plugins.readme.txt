A valid plugin should call pluginProcessor.registerPlugin.
It should define name to be valid.
Howeve, description is optional, and bufferHandler is optional.

pluginProcessor.registerPlugin({
    name: "test 1",
    description: "test 1",
    bufferHandler: editor => 
	editor.value = "First plugin operation",
});
